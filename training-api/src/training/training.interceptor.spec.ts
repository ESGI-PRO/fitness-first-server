import { TrainingInterceptor } from './training.interceptor';

describe('TrainingInterceptor', () => {
  it('should be defined', () => {
    expect(new TrainingInterceptor()).toBeDefined();
  });
});
