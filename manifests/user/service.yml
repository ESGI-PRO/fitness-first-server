apiVersion: v1
kind: Service
metadata:
  name: user
  namespace: "challengespace"
  #namespace: "ingress-nginx"
  labels:
    name: user
    app: user
spec:
  type: ClusterIP
  ports:
    - port: 8002
      targetPort: 80
  selector:
    app: user