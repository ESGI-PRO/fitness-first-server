apiVersion: v1
kind: Service
metadata:
  name: nutrition
  namespace: "challengespace"
  labels:
    name: nutrition
    app: nutrition
spec:
  type: NodePort
  ports:
    - port: 8001
      targetPort: 80
  selector:
    app: nutrition