apiVersion: v1
kind: Service
metadata:
  name: token
  namespace: "challengespace"
  #namespace: "ingress-nginx"
  labels:
    name: token
    app: token
spec:
  type: ClusterIP
  ports:
    - port: 8006
      targetPort: 80
  selector:
    app: token