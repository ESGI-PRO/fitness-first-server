apiVersion: v1
kind: Service
metadata:
  name: training
  namespace: "challengespace"
  #namespace: "ingress-nginx"
  labels:
    name: training
    app: training
spec:
  type: ClusterIP
  ports:
    - port: 8003
      targetPort: 80
  selector:
    app: training