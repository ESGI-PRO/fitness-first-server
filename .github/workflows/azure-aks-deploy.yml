name: Azure K8S deployment
on:
  workflow_run:
    workflows: ["Docker Image CI"]
    types:
      - completed
  push:
    branches: ["feature/testdocker"]
    paths:
      - ".github/workflows/azure-aks-deploy.yml"
      - "manifests/**"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
      - uses: azure/docker-login@v1
        with:
          login-server: challengeiw3.azurecr.io
          username: challengeiw3
          password: NQKUHlqpCO3r8l/jt0vlrsPmUnHDhNIAR9Q7MqcR6/+ACRDBVPUJ

      - run: |
          az login          
          az acr login -n challengeiw3
          az account set --subscription 3e16dd7c-61e5-4d26-946a-5e09d779e669
          az aks get-credentials --resource-group challenge_iw3 --name myAKSCluster
  
      - name: Build and push
        run: |
          docker build "./gateway-api" -f  "./gateway-api/Dockerfile" -t challengeiw3.azurecr.io/challenge-gateway:latest
          docker build "./analytic-api" -f  "./analytic-api/Dockerfile" -t challengeiw3.azurecr.io/challenge-analytic:latest
          docker build "./mailer-api" -f  "./mailer-api/Dockerfile" -t challengeiw3.azurecr.io/challenge-mailer:latest
          docker build "./messenger-api" -f  "./messenger-api/Dockerfile" -t challengeiw3.azurecr.io/challenge-messenger:latest
          docker build "./nutrition-api" -f  "./nutrition-api/Dockerfile" -t challengeiw3.azurecr.io/challenge-nutrition:latest
          docker build "./user-api" -f  "./user-api/Dockerfile" -t challengeiw3.azurecr.io/challenge-user:latest
          docker build "./permission-api" -f  "./permission-api/Dockerfile" -t challengeiw3.azurecr.io/challenge-permission:latest
          docker build "./subscription-api" -f  "./subscription-api/Dockerfile" -t challengeiw3.azurecr.io/challenge-subscription:latest
          docker build "./token-api" -f  "./token-api/Dockerfile" -t challengeiw3.azurecr.io/challenge-token:latest
          docker build "./training-api" -f  "./training-api/Dockerfile" -t challengeiw3.azurecr.io/challenge-training:latest
          docker push challengeiw3.azurecr.io/challenge-gateway:latest
          docker push challengeiw3.azurecr.io/challenge-analytic:latest
          docker push challengeiw3.azurecr.io/challenge-mailer:latest
          docker push challengeiw3.azurecr.io/challenge-messenger:latest
          docker push challengeiw3.azurecr.io/challenge-nutrition:latest
          docker push challengeiw3.azurecr.io/challenge-user:latest
          docker push challengeiw3.azurecr.io/challenge-permission:latest
          docker push challengeiw3.azurecr.io/challenge-subscription:latest
          docker push challengeiw3.azurecr.io/challenge-token:latest
          docker push challengeiw3.azurecr.io/challenge-training:latest

          az acr repository list --name challengeiw3 --output table
          az acr repository show-tags --name challengeiw3 --repository challenge-gateway --output table
          az acr repository show-tags --name challengeiw3 --repository challenge-analytic --output table
          az acr repository show-tags --name challengeiw3 --repository challenge-mailer --output table
          az acr repository show-tags --name challengeiw3 --repository challenge-messenger --output table
          az acr repository show-tags --name challengeiw3 --repository challenge-nutrition --output table
          az acr repository show-tags --name challengeiw3 --repository challenge-user --output table
          az acr repository show-tags --name challengeiw3 --repository challenge-permission --output table
          az acr repository show-tags --name challengeiw3 --repository challenge-subscription --output table
          az acr repository show-tags --name challengeiw3 --repository challenge-token --output table
          az acr repository show-tags --name challengeiw3 --repository challenge-training --output table


      - name: Deploy to AKS
        env: 
          AZURECRNAME: challengeiw3
          AZURECRUSERNAME: challengeiw3
          AZURECRPASSWORD: ${{ secrets.AZURE_CREDENTIALS }}
        run: |
          az aks get-credentials --resource-group challenge_iw3 --name myAKSCluster
          kubectl apply -f ./manifests/gateway/
          kubectl apply -f ./manifests/user/
          kubectl get pods -n challengespace
          kubectl get service -n challengespace
          kubectl get deployments -n challengespace
      # kubectl rollout restart deployment gateway -n ingress-nginx
      # kubectl rollout restart deployment analytic -n ingress-nginx
      # kubectl rollout restart deployment mailer -n ingress-nginx
      # kubectl rollout restart deployment messenger -n ingress-nginx
      # kubectl rollout restart deployment nutrition -n ingress-nginx
      # kubectl rollout restart deployment user -n ingress-nginx
      # kubectl rollout restart deployment permission -n ingress-nginx
      # kubectl rollout restart deployment subscription -n ingress-nginx
      # kubectl rollout restart deployment token -n ingress-nginx
      # kubectl rollout restart deployment training -n ingress-nginx
      # kubectl delete namespaces challengespace
      # kubectl create namespace challengespace